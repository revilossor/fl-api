{
  "name": "flowmanager-api",
  "version": "1.0.0",
  "description": "",
  "main": "server/index.js",
  "scripts": {
    "precommit": "npm t",
    "lint": "standard --fix",
    "unit": "stryker run",
    "audit": "npm audit",
    "test": "npm run lint && npm run unit && npm run audit",
    "dev": "ncc run server",
    "build": "ncc build server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@zeit/ncc": "^0.5.0",
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "mongoose": "^5.3.15",
    "pm2": "^3.2.2"
  },
  "devDependencies": {
    "husky": "^1.2.0",
    "jest": "^23.6.0",
    "standard": "^12.0.1",
    "stryker": "^0.33.1",
    "stryker-api": "^0.22.0",
    "stryker-javascript-mutator": "^0.12.1",
    "stryker-jest-runner": "^1.2.9",
    "supertest": "^3.3.0"
  },
  "standard": {
    "env": [
      "jest"
    ]
  }
}
